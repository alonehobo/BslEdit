#pragma once

#define IDI_APP_ICON 101

// The packed web interface (tools/pack-assets.mjs), linked into BSLView.wlx,
// BSLView.wlx64 and BSLEdit.exe so none of them needs a web\ directory beside it.
#define IDR_WEB_ASSETS 201
